import 'package:flutter/material.dart';

class CreateBoardHeader extends StatelessWidget {
  const CreateBoardHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
