import 'package:flutter/material.dart';

class CreateDocumentBody extends StatelessWidget {
  const CreateDocumentBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Create Document View',
        style: TextStyle(
          color: Colors.black,
          fontSize: 18,
        ),
      ),
    );
  }
}
