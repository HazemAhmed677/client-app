import 'package:flutter/material.dart';

class CommonDocItem extends StatelessWidget {
  const CommonDocItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
